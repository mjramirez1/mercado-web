{{#if productos}}
<div class="container">
    <div class="row">
        {{#each productos}}
        <div class="col-md-4 col-lg-3 col-sm-6">
            {{>Productos productos=this
            }}
        </div>
        {{/each}}
    </div>
</div>
{{/if}}