<div class="card m-5">
    <img src="/assets/img/{{productos.img}}" class="card-img-top" alt="{{productos.nombre}}">
    <div class="card-body">
        <h5 class="card-title">{{productos.nombre}}</h5>
        <p class="card-text">OFERTA: {{productos.precio}}</p>
        <a href="#" class="btn btn-primary">Añadir al carro</a>
    </div>
</div>